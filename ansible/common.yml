# setup common system applications and configure Ansible node
# ansible-playbook -i ./inventory/dev common.yml --extra-vars "env=mob env_type=prod user=user" -vv

- hosts: "{{env}}"
  user: "{{user}}"
  become: yes
  become_method: sudo
  gather_facts: true
  roles:
    - common
