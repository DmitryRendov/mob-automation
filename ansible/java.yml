# Setup Java
#
# ansible-playbook -i ./inventory/dev java.yml --extra-vars "env=mobsite-dev java_version=21 user=minecraft" -vv

- hosts: "{{env}}"
  user: "{{user}}"
  become: yes
  become_method: sudo
  gather_facts: true
  max_fail_percentage: 0

  roles:
    - java-oracle
