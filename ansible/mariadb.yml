# Setup MariaDB
#
# ansible-playbook -i ./inventory/live mariadb.yml --extra-vars "user=minecraft env=mobserver-vps"  -e "@local.vars.yml" -v

- hosts: "{{ env }}"
  user: "{{ user }}"
  become: yes
  become_method: sudo
  gather_facts: true
  max_fail_percentage: 0

  roles:
    - mariadb