# Setup MySQL server
# ansible-playbook -i ./inventory/dev mysql.yml --extra-vars "env=desktop mysql_engine=MyISAM" -vv

- hosts: "{{env}}"
  user: vagrant
  become: yes
  become_method: sudo
  gather_facts: true
  roles:
    - mysql

  vars_files:
    - host_vars/{{env}}
